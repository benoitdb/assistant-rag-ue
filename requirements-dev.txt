# Outillage de développement, pas nécessaire pour faire tourner l'application.
# Épinglé comme requirements.txt : la CI et le poste local doivent signaler les
# mêmes écarts de lint, sinon une PR verte en local échoue en CI (ou l'inverse).
-r requirements.txt
ruff==0.16.3
